const Alert = {
    acceptButtonText: {
        default: "Accept",
    },
    rejectButtonText: {
        default: "Reject",
    },
    okButtonText: {
        default: "Ok",
    },
};

export default Alert;
